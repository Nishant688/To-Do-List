# Frontend environment variables
# In development, the Vite dev server proxies /api to http://localhost:5000
# In production, set this to your hosted backend API URL (e.g., https://api.yourdomain.com/api)
VITE_API_URL=/api
